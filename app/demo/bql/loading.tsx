import { KhungTrang } from '@/components/khung-trang'

/** Màn chờ cho MỌI route dưới nhánh này — xem ghi chú ở KhungTrang. */
export default function Loading() {
  return <KhungTrang dang="bang" />
}
